import 'package:flutter/material.dart';// Step 3 컴포넌트
class StepSeats extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Step 3: 좌석 선택 클래스", style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)));
  }
}