import 'package:flutter/material.dart';
// Step 1 컴포넌트
class StepPayment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Step 1: 결제 클래스", style: TextStyle(fontSize: 24)));
  }
}