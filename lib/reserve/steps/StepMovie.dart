import 'package:flutter/material.dart';

// Step 1 컴포넌트
class StepMovie extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Step 1: 영화선택 클래스", style: TextStyle(fontSize: 24)));
  }
}