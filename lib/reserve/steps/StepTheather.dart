import 'package:flutter/material.dart';
// Step 2 컴포넌트
class StepTheather extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Step 2: 영화관 선택 클래스", style: TextStyle(fontSize: 24)));
  }
}